/*	=================== _traversal =================== 	Inorder tree traversal. To process a node, we use 	the function passed when traversal was called.	   Pre   Tree has been created (may be null) 	   Post  All nodes processed */void _avl_traversal (AVL_NODE* root, void (*process) (void* dataPtr)){//	Statements if  (root)    {     _avl_traversal  (root->left, process);     process     (root->dataPtr);     _avl_traversal  (root->right, process);    } // if return;}  // _traversal 